
function perimeterrectangle (a,b) {
        result=a*2+b*2;
        return result

}
console.log (perimeterrectangle (3,4))

function equality (x,y) {
    if (x==y) {
        console.log ("x and y are equal")
    }
    else {
        console.log ("x and y are not equal")
    }
}
console.log (equality (5,8))

function greatest(m,n) {
    if (m>n) {
        console.log ("m is the greatest number")
    }
    else if (m<n) {
        console.log ("n is the greatest number")
    }
    else {
        console.log ("m and n are equal numbers")
    }
}
console.log (greatest (6,11))

